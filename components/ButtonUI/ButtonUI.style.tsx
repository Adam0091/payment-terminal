import { Button, styled } from "@mui/material";

export const ColorButton = styled(Button)({
    color: "#ffffff",
    backgroundColor: "#4573a7bb",
    "&:hover": {
        backgroundColor: "#4573a7",
    },
});
